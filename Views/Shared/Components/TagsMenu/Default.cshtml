@model List<Tag>

<!--card yapısını buraya taşıdım-->
<div class="card mb-3">
    <div class="card-header">
        <h5 class="card-title mb-0">Tags</h5>
    </div>
    <div class="card-body">
        <ul class="list-inline d-flex flex-wrap gap-2">
            @foreach(var tag in Model){
                <li class="list-inline-item">
                    <a href="/posts/tag/@tag.Url" class="btn btn-outline-secondary btn-sm">@tag.Text</a><!--href ile urlyi alıyorum-->
                </li>
            }
        </ul>
    </div>
</div> 